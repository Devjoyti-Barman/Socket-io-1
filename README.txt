 First create a folder and open termina on it and do the following steps

Note : Generaly we need to install node and express and socket io

1. npm init
2. sudo install socket io express
3. Then add the files in that folder
4. open terminal and start server by " node server.js " without coloum   